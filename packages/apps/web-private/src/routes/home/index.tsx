import preact, { h } from 'preact';

import './style.scss';

const Home: preact.FunctionalComponent = () => (
    <div class="home-page">
        <h1>Home</h1>
        <p>This is the Home component.</p>
    </div>
);

export default Home;
