{
  "name": "root",
  "private": true,
  "scripts": {
    "serve": "lerna run serve:dev",
    "test": "lerna run test",
    "migration:up": "lerna run migration -- up",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
